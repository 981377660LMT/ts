export class Observable<T> {
  // ... implementation left as an exercise for the reader ...
}
declare function beforeAll(action: (done: boolean) => void, timeout?: number): void

beforeAll(() => {}, 1)
